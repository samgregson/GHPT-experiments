from setuptools import setup, find_packages

setup(
    name='GHPT-experiments',
    version='0.1',
    description='The aim is to "flow engineer" a Large Language Model (LLM) powered program to generate Grasshopper3d scripts based on a user prompt as input.',
    packages=find_packages(),
)
